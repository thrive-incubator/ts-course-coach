/** Window events that let independent hooks react to auth changes without shared context. */
export const SIGNED_IN_EVENT = 'ts-course-coach:signed-in';
export const SIGNED_OUT_EVENT = 'ts-course-coach:signed-out';
